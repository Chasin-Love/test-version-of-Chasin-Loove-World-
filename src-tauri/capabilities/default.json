{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. App commands (cosmos/store/realities) are always available to the core webview; this grants the Tauri core APIs the UI touches.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:event:default"
  ]
}
